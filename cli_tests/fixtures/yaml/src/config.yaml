version: 1.2.3
service:
  name: fixture-service
  enabled: true
  tags: [api, stable]
workers:
  - name: indexer
    settings:
      threads: 4
      queue: fast
  - name: notifier
    settings:
      threads: 2
      queue: reliable
message: |
  welcome to the fixture
  with multiple lines
