> apply 'lr src/patch.lua:3-4 "function M.status_text()\n"'
Applied 1 modification(s) across 1 stage(s) in snapshot mode.
=== src/patch.lua
local M = {}

-- TODO: remove after the release.
function M.status_text()
  return "pending"
end

function M.add(left, right)
  return left + right
end

return M
===
